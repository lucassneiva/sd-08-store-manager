const URL_MONGO_DATABASE = 'mongodb://localhost:27017/';
const NAME_OF_DATABASE = 'admin';
const OPTIONS = {
  useNewUrlParser: true,
  useUnifiedTopology: true,
};
const DEFAULT_PORT = 3000;
const MIN_LENGTH = 5;
const MIN_QUANTITY = 0;

module.exports = {
  URL_MONGO_DATABASE,
  NAME_OF_DATABASE,
  DEFAULT_PORT,
  OPTIONS,
  MIN_LENGTH,
  MIN_QUANTITY,
};
