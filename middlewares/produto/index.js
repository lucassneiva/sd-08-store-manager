const validaNome = require('./validaNome');
const validaNumero = require('./validaNumero');
const middlewareError = require('../middlewareError');
const validaId = require('./validaId');
const validaExiste = require('./validaExiste');

module.exports = {
  validaNome,
  validaNumero,
  middlewareError,
  validaId,
  validaExiste,
};
