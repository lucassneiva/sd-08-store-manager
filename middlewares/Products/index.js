const productValidations = require('./productValidadeMiddleware');
const requestValidations = require('./requestValidateMiddleware');

module.exports = {
  productValidations,
  requestValidations,
};
